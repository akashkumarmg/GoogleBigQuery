name: Gitleaks central foundry

on:
  pull_request:
    branches:
      - main
      
permissions:
  issues: write
  pull-requests: write
  contents: read
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
jobs:
  call-gitleaks:
    uses: akashkumarmg/git-central/.github/workflows/Gitleaks Central workflow foundry.yml@main
    secrets: inherit
