name: Gitleaks secret scan

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  gitleaks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set scan mode
        id: scan
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE="${{ github.event.pull_request.base.sha }}"
            HEAD="${{ github.event.pull_request.head.sha }}"
            echo "mode=pr" >> $GITHUB_OUTPUT
            echo "logopts=$BASE..$HEAD" >> $GITHUB_OUTPUT
          else
            echo "mode=main" >> $GITHUB_OUTPUT
            echo "logopts=" >> $GITHUB_OUTPUT  # No log opts = full scan of working dir
          fi

      - name: Run Gitleaks
        env:
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS }}
        run: |
          echo "Pulling Gitleaks Docker image..."
          docker pull zricethezav/gitleaks:latest

          MODE="${{ steps.scan.outputs.mode }}"
          LOG_OPTS="${{ steps.scan.outputs.logopts }}"

          if [[ "$MODE" == "pr" ]]; then
            echo "🔍 Scanning only the PR diff: $LOG_OPTS"
            docker run --rm \
              -v "${{ github.workspace }}:/repo" \
              -e GITLEAKS_LICENSE=$GITLEAKS \
              zricethezav/gitleaks:latest \
              detect --source=/repo \
              --log-opts="$LOG_OPTS" \
              --report-format=json \
              --report-path=/repo/gitleaks-results.json \
              --exit-code=0 || true
            cat gitleaks-results.json |jq
          else
            echo "🔍 Scanning full working directory of main branch (no history)"
            docker run --rm \
              -v "${{ github.workspace }}:/repo" \
              -e GITLEAKS_LICENSE=$GITLEAKS \
              zricethezav/gitleaks:latest \
              detect --source=/repo --no-git \
              --report-format=json \
              --report-path=/repo/gitleaks-results.json \
              --exit-code=0 || true
            cat gitleaks-results.json |jq
          fi

      - name: Comment findings on PR
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB }}
        run: |
          FINDINGS_JSON=$(cat gitleaks-results.json)
          COUNT=$(echo "$FINDINGS_JSON" | jq 'length')
          SHA="${{ github.event.pull_request.head.sha }}"
          REPO="${{ github.repository }}"
          PR_NUMBER="${{ github.event.pull_request.number }}"
          MAX=10
          if [ "$COUNT" -gt 0 ]; then
            COMMENT="**🔐 Gitleaks Findings: $COUNT issue(s) detected**\n\n"
            i=0
            while [ "$i" -lt "$COUNT" ] && [ "$i" -lt "$MAX" ]; do
              ITEM=$(echo "$FINDINGS_JSON" | jq ".[$i]")
              RULE=$(echo "$ITEM" | jq -r '.RuleID')
              DESC=$(echo "$ITEM" | jq -r '.Description')
              FILE=$(echo "$ITEM" | jq -r '.File')
              LINE=$(echo "$ITEM" | jq -r '.Line')
              LINK="https://github.com/$REPO/blob/$SHA/$FILE#L$LINE"
              SECRET_MASKED="**********"
              COMMENT+="🔸 **Rule**: \`$RULE\`\n"
              COMMENT+="📄 **File**: \`$FILE:$LINE\`\n"
              COMMENT+="📝 **Description**: $DESC\n"
              COMMENT+="🔑 **Secret**: \`$SECRET_MASKED\`\n"
              COMMENT+="🔗 **Path**: [$FILE:$LINE]($LINK)\n\n"
              i=$((i + 1))
            done
            if [ "$COUNT" -gt "$MAX" ]; then
              COMMENT+="...and more. Only showing first $MAX findings.\n"
            fi
          else
            COMMENT="✅ **Gitleaks Findings:** No secrets detected. Safe to proceed!"
          fi
          COMMENT=$(echo "$COMMENT" | sed ':a;N;$!ba;s/\n/\\n/g')
          curl -X POST \
            -H "Authorization: token $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            -d "{\"body\":\"$COMMENT\"}" \
            "https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments"
